version: "2"
sql:
  - schema: "schema.sql"
    queries: "queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "../domain/db"
        sql_package: "pgx/v5"
        emit_json_tags: true